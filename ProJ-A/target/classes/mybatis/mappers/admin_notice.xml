<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.admin.notice">

		<resultMap id="noticeResult" type="NoticeVO">
		<result property="bno" column="bno" />
		<result property="title" column="title" />
		<result property="content" column="content" />
		<result property="writer" column="writer" />
		<result property="regdate" column="regdate" />
		<result property="updatedate" column="updatedate" />
	</resultMap>

<select id="selectAllNoticeList" resultMap="noticeResult">
	<![CDATA[
		select bno,writer,title,content from notice_board
		order by regdate asc
	]]>
</select>
 <insert id="insertList" parameterType="NoticeVO">
	   INSERT INTO NOTICE_BOARD(    BNO 
	   					      , TITLE 
	   					      , CONTENT 
	   					      , WRITER  )
	                 VALUES(    SEQ_NOTICE_ID.NEXTVAL 
	                 		  , #{title}
	                 		  , #{content}
	                 		  , #{writer}  )
   
   </insert>
</mapper>